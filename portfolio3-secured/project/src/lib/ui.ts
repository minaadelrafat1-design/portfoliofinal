/**
 * Shared style tokens for the admin dashboard.
 * Centralized here to avoid repeating identical Tailwind class strings
 * across every admin form component.
 */
export const ADMIN_INPUT_CLS =
  'w-full rounded-lg border border-slate-700 bg-slate-800 px-3 py-2 text-sm outline-none focus:border-primary';
